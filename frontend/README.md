# PitCrew Frontend

Placeholder for the PitCrew web frontend.

No code lives here yet - this directory is scaffolded in Phase 0 so the monorepo layout (`backend/` + `frontend/`) is in place before frontend work starts in a later phase, per `plan.md` at the repo root.
